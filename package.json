{
  "name": "purescript-reactors-template",
  "version": "1.0.0",
  "main": "dev/index.js",
  "scripts": {
    "start": "parcel serve dev/index.html",
    "spago-install": "spago install",
    "spago-build": "spago build",
    "build-prod": "mkdir -p prod && cp dev/index.html prod/ && rm -rf dist && spago bundle-app --to prod/index.js && parcel build prod/index.html"
  },
  "license": "MIT",
  "devDependencies": {
    "autoprefixer": "^10.3.4",
    "parcel": "^2.0.0-rc.0",
    "postcss": "^8.3.6",
    "purescript": "^0.14.5",
    "purs-tidy": "^0.5.2",
    "spago": "^0.20.3",
    "tailwindcss": "^2.2.15"
  }
}
